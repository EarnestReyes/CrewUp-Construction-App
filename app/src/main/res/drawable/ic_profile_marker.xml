<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background circle -->
    <item>
        <shape android:shape="oval">
            <solid android:color="?attr/colorPrimary"/>
        </shape>
    </item>

    <!-- Person icon -->
    <item
        android:drawable="@drawable/ic_profile_placeholder_foreground"
        android:gravity="center"
        android:width="32dp"
        android:height="32dp"/>
</layer-list>
